# secrets / generated config
.env
docker-compose.override.yml
# Server.xml/Logger.xml contain no literal secrets (${env:...} substitution only) — tracked in git.

# host-specific default data dirs (only used if RECORDINGS_DIR/VOD_DIR aren't
# overridden in .env — this deployment always overrides them, see .env)
/data/

# runtime data
ome/logs/
console/data/*.sqlite
console/data/*.sqlite-*

# Sprint 8b: local backup archives (contain secrets from .env) — never commit
backups/

# node / next.js
console/node_modules/
console/.next/
console/npm-debug.log*

# misc
.DS_Store
